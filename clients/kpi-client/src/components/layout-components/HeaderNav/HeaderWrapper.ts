import styled from "@emotion/styled";

const HeaderWrapper = styled.div(() => ({
  width: "100%",
  display: "flex",
  alignItems: "center",
  overflow: "hidden",
  minWidth: 0,
}));

export default HeaderWrapper;
